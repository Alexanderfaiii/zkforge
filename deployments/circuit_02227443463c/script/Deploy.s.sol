// ZKForge Deployment Script
// Generated by ZKForge
//
// Usage:
//   forge script deploy.s.sol --rpc-url http://localhost:8545 --broadcast --verify
//
// Or:
//   forge create circuit_02227443463cVerifier --rpc-url http://localhost:8545 --private-key $PRIVATE_KEY

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "forge-std/Script.sol";
import "./circuit_02227443463cVerifier.sol";

contract DeployScript is Script {
    function run() external {
        uint256 deployerPrivateKey = vm.envUint("PRIVATE_KEY");
        vm.startBroadcast(deployerPrivateKey);

        circuit_02227443463cVerifier verifier = new circuit_02227443463cVerifier();

        vm.stopBroadcast();

        console.log("circuit_02227443463cVerifier deployed at:", address(verifier));
        console.log("Chain ID:", 11155111);
        console.log("RPC: http://localhost:8545");
    }
}
